repl:
  enabled: true
  node_id: "2"
  node_name: "Target Node"

  postgresql:
    host: localhost
    port: 15433
    database: testdb
    user: test
    sslmode: disable

  grpc:
    port: 15461

  ipc:
    enabled: true
    path: /tmp/steep-repl-target.sock

  http:
    enabled: false
    port: 8080

debug: true
