repl:
  enabled: true
  node_id: "source-node"
  node_name: "Source Node"

  postgresql:
    host: localhost
    port: 15432
    database: testdb
    user: test
    sslmode: disable

  grpc:
    port: 15460

  ipc:
    enabled: true
    path: /tmp/steep-repl-source.sock

  http:
    enabled: false

debug: true
